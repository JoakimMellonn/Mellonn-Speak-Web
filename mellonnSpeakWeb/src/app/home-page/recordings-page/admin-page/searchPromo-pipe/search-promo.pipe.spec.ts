import { SearchPromoPipe } from './search-promo.pipe';

describe('SearchPromoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPromoPipe();
    expect(pipe).toBeTruthy();
  });
});
